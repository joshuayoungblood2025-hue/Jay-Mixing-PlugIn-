"""Phase 3 learning: corpus-derived targets and a preference/feedback model.

Pure standard-library statistical learning (medians, weighted averages) — no numpy or ML
frameworks. The goal is transparent, data-informed targets and personalization that plug
into the existing engine interfaces, not a black-box model.
"""
